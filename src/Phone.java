public class Phone {

    // Abstract methods -> Which are just declared you don't need to write the implementation of it
    // Child class will write its implementation on its own
    // We are just telling all the child classes that you should have this call method implement it in
    // your own way
    int a = 10;
    // If a class is abstract what does it mean ?
    // It means that you can not create the object of that class.
    public  void call(){};
    public  void dance(){};
}
