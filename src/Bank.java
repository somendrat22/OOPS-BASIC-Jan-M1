public interface Bank {
    // all the methods are by default abstract
    // We use interface in desiegning
    // Every method in interfaces is by default public and abstract
    // Every variable in interfaces are final and static
    // Q1 -> Can i use this vari
    final static int x = 10;
    void deposit();
    void withdraw();
}
